<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
    />
  </head>
  <body>
    <ul class="nav mb-3">
      <li class="nav-item"><a href="/" class="nav-link">Home </a></li>
      <li class="nav-item"><a href="/display" class="nav-link">Display </a></li>
      <li class="nav-item"><a href="/edit" class="nav-link">Edit </a></li>
    </ul>
    <div class="container">
      <div class="row g-3">
        <% for( let index = 0; index < task.length; index++ ) { %>
        <div class="border rounded-3 col-12 col-sm-6 col-md-4 col-lg-3 p-3">
          <h3><%= task[index].task %></h3>
          <p><%= task[index].description %></p>
          <div class="d-flex">
              
              <button class="btn btn-warning"> Edit</button>
              <form action="/delete" method="post">
                  <input type="hidden" name="index" value=<%= task[index]._id%>>
                  <button class="btn btn-danger"> Delete</button>
              </form>

          </div>
        </div>
        <% } %>
      </div>
    </div>
  </body>
</html>
